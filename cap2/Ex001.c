#include <stdio.h>
#include <stdlib.h>
//1 - Write a program that prints the messages “Início do programa” and “Fim” on the screen, one per line, using only one printf() command.
int main(){
    printf("Inicio do programa\nFim");

    return 0;
}
